<resources>
    <!-- Base application theme. -->
    <style name="Base" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!--        -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--        -->
        <item name="myBackground">@color/blue</item>
        <item name="android:colorBackground">?attr/myBackground</item>
        <!--        -->
        <item name="myForeground">@color/white</item>
        <item name="colorOnBackground">?attr/myForeground</item>
        <item name="android:textColorPrimary">?attr/myForeground</item>
    </style>
</resources>